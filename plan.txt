1. Implement performance optimization for distance calculation in CompanionSynergyCard.kt by avoiding expensive `sqrt()` operations. This can be achieved by doing squared distance comparison. `val distSq = (item1.x - item2.x) * (item1.x - item2.x) + (item1.y - item2.y) * (item1.y - item2.y)` and then checking `distSq <= 2.25`. This also includes the one in RestorationJournalScreen.kt and GardenViewModel.kt.
