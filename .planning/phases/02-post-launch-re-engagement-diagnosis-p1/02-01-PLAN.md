---
phase: 02-post-launch-re-engagement-diagnosis-p1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/data/model/AssessmentResult.kt
  - app/src/main/java/com/example/data/model/GardenEntities.kt
  - app/src/main/java/com/example/data/database/GardenDao.kt
  - app/src/main/java/com/example/data/database/GardenDatabase.kt
  - app/src/main/java/com/example/data/repository/GardenRepository.kt
  - app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt
  - app/src/main/res/layout/garden_widget.xml
  - app/src/main/java/com/example/ui/screens/dashboard/GardenWidgetProvider.kt
  - app/src/main/java/com/example/ui/screens/dashboard/BiophilicProfileCard.kt
  - app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt
  - app/src/main/java/com/example/ui/screens/AiStudioScreen.kt
autonomous: true
requirements:
  - WIDGET-01
  - DIAG-01
  - REASSESS-01
  - HISTORY-01
must_haves:
  truths:
    - "AssessmentResult database entity is created and integrated with GardenDatabase and GardenDao."
    - "GardenViewModel stores assessment results in Room, exposes history flow, tracks step progress checkboxes, and supports Space Diagnosis mode."
    - "GardenWidgetProvider updates the home screen widget with the latest Neural Load score and completed next steps progress."
    - "DashboardScreen displays the 30-day reassessment banner, interactive next steps checklist, and a visual history trend line chart."
    - "AiStudioScreen includes a quick suggestion button to start a detailed conversational Space Diagnosis."
  artifacts:
    - "app/src/main/java/com/example/data/model/AssessmentResult.kt"
    - "app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt"
  key_links:
    - "GardenViewModel manages database history and shared preferences flags"
    - "GardenWidgetProvider binds assessment metrics to RemoteViews"
---

<objective>
Implement home screen widgets, history tracking, 30-day reassessment prompts, and conversational space diagnosis to complete Phase 2 requirements.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database & Repository Updates</name>
  <files>
    - app/src/main/java/com/example/data/model/AssessmentResult.kt
    - app/src/main/java/com/example/data/model/GardenEntities.kt
    - app/src/main/java/com/example/data/database/GardenDao.kt
    - app/src/main/java/com/example/data/database/GardenDatabase.kt
    - app/src/main/java/com/example/data/repository/GardenRepository.kt
  </files>
  <read_first>
    - app/src/main/java/com/example/data/model/GardenEntities.kt
    - app/src/main/java/com/example/data/database/GardenDao.kt
    - app/src/main/java/com/example/data/database/GardenDatabase.kt
  </read_first>
  <action>
    1. Create `AssessmentResult.kt` defining Room Entity `AssessmentResult` with fields: `id` (int, autoGenerate primary key), `timestamp` (long), `score` (int), and `lowestCategories` (string).
    2. Add Dao interface methods in `GardenDao.kt` to insert and query all assessment results sorted descending by timestamp.
    3. Update `GardenDatabase.kt` to include `AssessmentResult::class` in the list of database entities and bump the database version to 7.
    4. Expose the queries in `GardenRepository.kt`.
  </action>
  <verify>Check compilation and that database version is bumped correctly.</verify>
  <acceptance_criteria>
    - `AssessmentResult.kt` compiles successfully as a Room entity.
    - `GardenDatabase` lists `AssessmentResult` in the database entities array.
    - `GardenDao` has queries for inserting and selecting assessment results.
  </acceptance_criteria>
  <done>Assessment database table and repository access methods are complete.</done>
</task>

<task type="auto">
  <name>Task 2: View Model & State Layer Updates</name>
  <files>
    - app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt
  </files>
  <read_first>
    - app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt
  </read_first>
  <action>
    1. Add Flow retrieval of `allAssessmentResults` in `GardenViewModel.kt`.
    2. Update `saveAssessmentResult` to insert an `AssessmentResult` record to the database, set `last_assessment_timestamp` in `sharedPrefs`, and reset step completion preferences to `false`.
    3. Expose step completion flows: `step1Completed`, `step2Completed`, `step3Completed`, and a toggle method `toggleStepCompleted(index: Int)`.
    4. Calculate `needsReassessment` Flow checking if `System.currentTimeMillis() - last_assessment_timestamp >= 30 days` (or simulation flag is active). Expose simulation toggle action.
    5. Handle Space Diagnosis mode: add a StateFlow `isSpaceDiagnosisMode`. When active, customize Dr. Julian's system instructions to guide the user through room biophilic assessment questions.
  </action>
  <verify>Verify viewmodel compiles and contains correct state flows and preference setters.</verify>
  <acceptance_criteria>
    - `GardenViewModel` saves assessment scores into Room database.
    - `GardenViewModel` exposes step completion checkboxes and reassessment timing states.
  </acceptance_criteria>
  <done>Viewmodel state flows and business logic for Phase 2 features are complete.</done>
</task>

<task type="auto">
  <name>Task 3: AppWidget Layout & Provider updates</name>
  <files>
    - app/src/main/res/layout/garden_widget.xml
    - app/src/main/java/com/example/ui/screens/dashboard/GardenWidgetProvider.kt
  </files>
  <read_first>
    - app/src/main/res/layout/garden_widget.xml
    - app/src/main/java/com/example/ui/screens/dashboard/GardenWidgetProvider.kt
  </read_first>
  <action>
    1. Add TextViews for Neural Load score (`@id/widget_neural_load`) and next steps progress (`@id/widget_steps_progress`) inside `garden_widget.xml`.
    2. Modify `GardenWidgetProvider.kt` to retrieve the latest assessment score and next steps progress in `runBlocking` and bind them to the views.
  </action>
  <verify>Verify widget layout compiles and widget provider updates views successfully.</verify>
  <acceptance_criteria>
    - `garden_widget.xml` contains text views for Neural Load and next steps progress.
    - `GardenWidgetProvider` queries Room and sharedPrefs to update these views.
  </acceptance_criteria>
  <done>Home screen widget updates for biophilic scores and step progress are complete.</done>
</task>

<task type="auto">
  <name>Task 4: UI Screens Presentation updates</name>
  <files>
    - app/src/main/java/com/example/ui/screens/dashboard/BiophilicProfileCard.kt
    - app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt
    - app/src/main/java/com/example/ui/screens/AiStudioScreen.kt
  </files>
  <read_first>
    - app/src/main/java/com/example/ui/screens/dashboard/BiophilicProfileCard.kt
    - app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt
    - app/src/main/java/com/example/ui/screens/AiStudioScreen.kt
  </read_first>
  <action>
    1. Update `BiophilicProfileCard.kt` to render interactive checkboxes for the 3 next steps, updating viewmodel progress.
    2. Update `DashboardScreen.kt` to display a 30-day reassessment banner, a simulation trigger toggle, a link to trigger Space Diagnosis, and a beautiful Canvas line chart showing past scores trend.
    3. Update `AiStudioScreen.kt` to include `"🔍 Run a detailed Space Diagnosis for my room"` suggesting trigger, initiating Guided Diagnosis mode.
  </action>
  <verify>Ensure compilation and check rendered layout details in compose.</verify>
  <acceptance_criteria>
    - Checklist and trend chart display on Dashboard screen.
    - Reassessment prompt displays when simulated or active.
    - Space diagnosis trigger button displays on AI screen.
  </acceptance_criteria>
  <done>Compose UI updates for Phase 2 are complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `./gradlew assembleDebug` to ensure compilation passes.
- [ ] Run `./gradlew testDebugUnitTest` to run the unit tests and verify there are no compile or runtime errors.
</verification>

<success_criteria>
- Compact score widget on the home screen shows Neural Load and step progress.
- Conversational Space Diagnosis guides the user through biophilic recommendations.
- Reassessment prompt appears when 30 days have elapsed.
- Historical scores are displayed in a trend chart and list.
</success_criteria>

<output>
After completion, create `.planning/phases/02-post-launch-re-engagement-diagnosis-p1/02-01-SUMMARY.md`
</output>
