---
phase: 01-neural-load-assessment-ai-advisor-p0
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/ui/screens/walkthrough/WalkthroughOverlay.kt
autonomous: true
requirements:
  - AR-01
must_haves:
  truths:
    - "The onboarding walkthrough overlay button shows 'Next' (with an arrow icon) on the AI Advisor tab step."
    - "The onboarding walkthrough overlay button only shows 'Finish' (with a check icon) on the final (Restoration Journal) tab step."
  artifacts:
    - "app/src/main/java/com/example/ui/screens/walkthrough/WalkthroughOverlay.kt"
  key_links:
    - "WalkthroughOverlay handles button label and icon conditions based on the actual final step of the walkthrough"
---

<objective>
Fix the walkthrough flow mismatch where the button on the AI Advisor step incorrectly displays "Finish" instead of "Next" before transitioning to the Restoration Journal step.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/example/ui/screens/walkthrough/WalkthroughOverlay.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update finish label and check icon conditions in WalkthroughOverlay</name>
  <files>app/src/main/java/com/example/ui/screens/walkthrough/WalkthroughOverlay.kt</files>
  <read_first>app/src/main/java/com/example/ui/screens/walkthrough/WalkthroughOverlay.kt</read_first>
  <action>
    In `WalkthroughOverlay.kt`, find where `buttonText` is defined:
    ```kotlin
    val buttonText = if (currentStep == WalkthroughStep.AI_ADVISOR_TAB || currentStep == WalkthroughStep.AR_LENS_TAB) "Finish" else "Next"
    ```
    Change it to:
    ```kotlin
    val buttonText = if (currentStep == WalkthroughStep.AR_LENS_TAB) "Finish" else "Next"
    ```
    
    Similarly, find where the check icon `imageVector` is defined:
    ```kotlin
    imageVector = if (currentStep == WalkthroughStep.AI_ADVISOR_TAB || currentStep == WalkthroughStep.AR_LENS_TAB) Icons.Default.Check else Icons.Default.ArrowForward
    ```
    Change it to:
    ```kotlin
    imageVector = if (currentStep == WalkthroughStep.AR_LENS_TAB) Icons.Default.Check else Icons.Default.ArrowForward
    ```
  </action>
  <verify>Verify button labels and icons for all walkthrough steps.</verify>
  <acceptance_criteria>
    - `WalkthroughOverlay.kt` shows "Next" on the AI Advisor step.
    - `WalkthroughOverlay.kt` shows "Finish" on the Restoration step.
  </acceptance_criteria>
  <done>Walkthrough label and icon conditions are updated to reflect the actual walkthrough sequence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `./gradlew assembleDebug` to ensure compilation passes.
- [ ] Run `./gradlew testDebugUnitTest` to run the unit tests and verify there are no compile or runtime errors.
</verification>

<success_criteria>
- The button on the AI Advisor tab walkthrough step shows "Next".
- The button on the Restoration Journal tab walkthrough step shows "Finish".
</success_criteria>

<output>
After completion, create `.planning/phases/01-neural-load-assessment-ai-advisor-p0/01-02-SUMMARY.md`
</output>
