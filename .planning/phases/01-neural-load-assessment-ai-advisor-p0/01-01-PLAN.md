---
phase: 01-neural-load-assessment-ai-advisor-p0
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt
  - app/src/main/java/com/example/ui/screens/OnboardingScreen.kt
  - app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt
  - app/src/main/java/com/example/MainActivity.kt
autonomous: true
requirements:
  - ASSESS-01
  - RESULT-01
  - STEPS-01
  - ADVISOR-01
  - AR-01
must_haves:
  truths:
    - "User can view the new onboarding screen containing the welcome screen and take the 10-question assessment flow."
    - "User sees their score, zone background color, and personalized next steps after taking the assessment."
    - "User can skip the assessment and see the gentle reminder banner on the dashboard."
    - "AI Advisor default system prompt is updated to Biophilic Design Advisor."
    - "AR Lens tab is hidden from bottom navigation."
  artifacts:
    - "app/src/main/java/com/example/ui/screens/OnboardingScreen.kt"
    - "app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt"
    - "app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt"
    - "app/src/main/java/com/example/MainActivity.kt"
  key_links:
    - "OnboardingScreen triggers saveAssessmentResult in GardenViewModel when finished"
---

<objective>
Implement the P0 features of Phase 1: Neural Load Assessment and AI Advisor Reframe. This includes the new onboarding flow, result screens, personalized next steps, biophilic AI Advisor updates, and removing the temporary AR Lens navigation tab.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt
@app/src/main/java/com/example/ui/screens/OnboardingScreen.kt
@app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt
@app/src/main/java/com/example/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GardenViewModel to support Assessment State, Hoisted Navigation, and Biophilic AI Mode</name>
  <files>app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt</files>
  <read_first>app/src/main/java/com/example/ui/viewmodel/GardenViewModel.kt</read_first>
  <action>
    Add the following state variables to GardenViewModel:
    - `_assessmentScore` (MutableStateFlow<Int?>(null))
    - `_lowestCategories` (MutableStateFlow<List<String>>(emptyList()))
    - `_isAssessmentSkipped` (MutableStateFlow(false))
    - `_currentTab` (MutableStateFlow(0)) - to hoist tab index state.

    Add these methods:
    - `saveAssessmentResult(score: Int, lowest: List<String>)` - sets score, lowest categories, sets skipped to false, onboarding_completed to true, and saves to SharedPreferences.
    - `skipAssessment()` - sets skipped to true, onboarding_completed to true, saves to SharedPreferences.
    - `resetAssessment()` - clears score, lowest categories, skipped status, sets onboarding_completed to false, saves to SharedPreferences.
    - `setCurrentTab(tab: Int)` - updates `_currentTab` value.

    Load values in init from SharedPreferences:
    - `assessment_score` (default -1)
    - `lowest_categories` (default "")
    - `assessment_skipped` (default false)

    Update AI chat logic (`sendAiChatMessage`):
    - Build `systemInstruction` dynamically: If `_assessmentScore.value != null`, inject the Neural Load score/zone, and lowest-scoring categories, and instruct the model to connect recommendations to biology, use specific plants/configurations, and end with CTAs to other features.
  </action>
  <verify>Compile and run unit tests.</verify>
  <acceptance_criteria>
    - `GardenViewModel.kt` contains `saveAssessmentResult`
    - `GardenViewModel.kt` contains `skipAssessment`
    - `GardenViewModel.kt` contains `resetAssessment`
    - `GardenViewModel.kt` contains `setCurrentTab`
  </acceptance_criteria>
  <done>ViewModel compiles and supports new assessment flows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 6-screen Neural Load Assessment in OnboardingScreen</name>
  <files>app/src/main/java/com/example/ui/screens/OnboardingScreen.kt</files>
  <read_first>app/src/main/java/com/example/ui/screens/OnboardingScreen.kt</read_first>
  <action>
    Rewrite `OnboardingScreen.kt` to present a 6-stage flow:
    - Stage 1: Welcome/Splash: Title, subtext, sand-colored "Start My Assessment" CTA, "Skip for now →" link (calls skipAssessment()).
    - Stage 2: Assessment Questions (10 screens total): Progress bar with 10 segments. Question header, Sage green category pill (e.g. NATURE VIEWS), question text, and 3 full-width card touch targets for the choices. Tapping immediately advances with a smooth slide transition. Swipe back/back tap allowed.
    - Stage 3: Calculating Screen: Leafs pulsing animation, text cycles ("Analyzing your environment...", "Calculating Neural Load...", "Generating your results..."). Pauses for 2 seconds before automatically transitioning to Results.
    - Stage 4: Results Screen: Large score "X / 20" (48pt+ bold). Zone badge/pill with background color matching the zone (Green, Yellow, Red). Summary paragraph explaining their zone. Two stacked buttons: "See My 3 Next Steps" (primary) and "Share My Score" (secondary).
    - Stage 5: Next Steps: List of 3 cards representing their lowest-scoring categories. Each card is expandable and contains a description and a deep link CTA (e.g., "Find plants for my space" -> tab 3, "Design my layout" -> tab 1, "Browse material ideas" -> tab 2) that navigates to the tab and completes onboarding (calling saveAssessmentResult()).
  </action>
  <verify>Run the app and manually step through the onboarding assessment flow.</verify>
  <acceptance_criteria>
    - `OnboardingScreen.kt` contains Splash, Questions, Calculating, Results, and Next Steps UI states.
  </acceptance_criteria>
  <done>Assessment onboarding flow is fully interactive and stores results.</done>
</task>

<task type="auto">
  <name>Task 3: Implement skipped assessment banner on DashboardScreen</name>
  <files>app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt</files>
  <read_first>app/src/main/java/com/example/ui/screens/dashboard/DashboardScreen.kt</read_first>
  <action>
    Read `isAssessmentSkipped` state from `GardenViewModel` in `DashboardScreen.kt`.
    If true, render a prominent but gentle warning banner at the top of the dashboard:
    - Background: Light amber or warm yellow accent.
    - Text: "You haven't taken your Neural Load assessment yet. Take a 2-minute assessment to personalize your space."
    - Button: "Take Assessment" (calls `viewModel.resetAssessment()` to reset onboarding completed status, redirecting the user back to the onboarding assessment screen).
  </action>
  <verify>Verify banner visibility when assessment is skipped.</verify>
  <acceptance_criteria>
    - `DashboardScreen.kt` displays a banner when `isAssessmentSkipped` is true.
  </acceptance_criteria>
  <done>Reminder banner displays correctly when the assessment flow is skipped.</done>
</task>

<task type="auto">
  <name>Task 4: Update MainActivity to handle hoisted navigation and hide the AR Lens tab</name>
  <files>app/src/main/java/com/example/MainActivity.kt</files>
  <read_first>app/src/main/java/com/example/MainActivity.kt</read_first>
  <action>
    Modify `MainActivity.kt`:
    - Bind `currentTab` state variable to `viewModel.currentTab.collectAsState()`.
    - Remove the bottom navigation tab item for AR Lens (tab index 4) from the `NavigationBar` to hide the feature.
    - Remove any walkthrough references to AR Lens to avoid crashes.
  </action>
  <verify>Verify that bottom navigation only has 4 tabs and AR Lens is hidden.</verify>
  <acceptance_criteria>
    - `MainActivity.kt` uses `viewModel.currentTab` state flow.
    - bottom navigation bar has only 4 items (Dashboard, 2D Planner, Greenhouse, AI Advisor).
  </acceptance_criteria>
  <done>AR Lens is hidden and navigation is dynamically managed by the ViewModel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `./gradlew assembleDebug` to ensure compilation passes.
- [ ] Run `./gradlew testDebugUnitTest` to run the unit tests and verify there are no compile or runtime errors.
</verification>

<success_criteria>
- All tasks completed.
- Onboarding screen behaves as a 6-stage Neural Load assessment.
- Assessment score and categories are persisted in SharedPreferences.
- If assessment is skipped, a banner is shown on the dashboard.
- AI Advisor default prompt uses biophilic guidelines based on scores.
- AR Lens tab is removed from the bottom bar.
</success_criteria>

<output>
After completion, create `.planning/phases/01-neural-load-assessment-ai-advisor-p0/01-01-SUMMARY.md`
</output>
